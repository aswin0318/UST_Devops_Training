apiVersion: v1
kind: Pod
metadata:
  name: request-only-overuse
spec:
  containers:
  - name: stress
    image: polinux/stress
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "2G", "--vm-hang", "1"]
    resources:
      requests:
        memory: "500Mi"
        cpu: "200m"
