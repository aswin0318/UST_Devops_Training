apiVersion: v1
kind: Pod
metadata:
  name: pod1
spec:
  containers:
  - name: stress
    image: polinux/stress
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "700M", "--vm-hang", "1"]
    resources:
      requests:
        memory: "500Mi"
      limits:
        memory: "800Mi"
