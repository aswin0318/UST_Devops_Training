apiVersion: v1
kind: Pod
metadata:
  name: too-big-pod
spec:
  containers:
  - name: stress
    image: polinux/stress
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "1G", "--vm-hang", "1"]
    resources:
      requests:
        memory: "600Mi"
      limits:
        memory: "1G"
