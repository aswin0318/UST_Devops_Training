apiVersion: v1
kind: Pod
metadata:
  name: pod3
spec:
  containers:
  - name: stress
    image: polinux/stress
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "400M", "--vm-hang", "1"]
    resources:
      requests:
        memory: "400Mi"
      limits:
        memory: "500Mi"
